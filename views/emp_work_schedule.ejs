<!DOCTYPE html>
<html lang="th"></html>
<head>
  <meta charset="UTF-8">
  <title>SSBeauty ตารางงาน</title>
  <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="bg-gray-50 min-h-screen m-0">
    <%- include('header_emp') %>
    
    <!-- Main Content -->
    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-6">
        <!-- Page Header with Filter and Summary -->
        <div class="mb-6">
            <h1 class="text-2xl font-bold text-gray-900 mb-1">ตารางงานวันนี้</h1>
            <p class="text-sm text-gray-600">รายการนัดหมายและรายได้ของวันนี้</p>
        </div>

        <!-- Filter and Summary Row -->
        <div class="flex flex-col lg:flex-row gap-4 mb-6">
            <!-- Quick Stats Panel -->
            <div class="flex-1 bg-white shadow-sm rounded-lg border border-gray-200 p-4">
                <h3 class="text-base font-semibold text-gray-900 mb-3">สรุปงานวันนี้</h3>
                <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                    <div class="text-center">
                        <div class="text-2xl font-bold text-blue-600"><%= appointments.filter(a => a.status === 'booked').length %></div>
                        <div class="text-xs text-gray-600 mt-1">รอเริ่มงาน</div>
                    </div>
                    <div class="text-center">
                        <div class="text-2xl font-bold text-orange-600"><%= appointments.filter(a => a.status === 'in_progress').length %></div>
                        <div class="text-xs text-gray-600 mt-1">กำลังทำ</div>
                    </div>
                    <div class="text-center">
                        <div class="text-2xl font-bold text-green-600"><%= appointments.filter(a => a.status === 'completed').length %></div>
                        <div class="text-xs text-gray-600 mt-1">เสร็จแล้ว</div>
                    </div>
                    <div class="text-center">
                        <div class="text-2xl font-bold text-[#A5848F]"><%= appointments.length %></div>
                        <div class="text-xs text-gray-600 mt-1">ทั้งหมด</div>
                    </div>
                </div>
            </div>

            <!-- Summary Card -->
            <div class="lg:w-64">
                <div class="bg-white shadow-sm rounded-lg border border-gray-200 p-5">
                    <p class="text-sm font-medium text-gray-600 mb-3">รายได้วันนี้</p>
                    <p class="text-2xl font-bold text-[#A5848F]"><%= totalIncome.toLocaleString('th-TH', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %> บาท</p>
                    <p class="text-xs text-gray-500 mt-1">จากงานที่เสร็จสิ้น <%= appointments.filter(a => a.status === 'completed').length %> งาน</p>
                </div>
            </div>
        </div>

        <!-- Appointments List -->
        <div class="space-y-3">
            <% if (appointments.length === 0) { %>
                <div class="bg-white shadow-sm rounded-lg p-8 border border-gray-200 text-center">
                    <p class="text-gray-500 text-lg">ไม่มีนัดหมายสำหรับวันนี้</p>
                </div>
            <% } else { %>
            <% appointments.forEach((appointment, index) => { %>
                <% 
                    date = new Date(appointment.appointment_date);
                    const hours = date.getHours();
                    const minutes = date.getMinutes();
                    const formattedHours = hours < 10 ? '0' + hours : hours;
                    const formattedMinutes = minutes < 10 ? '0' + minutes : minutes;
                    const hhmm = `${formattedHours}:${formattedMinutes}`;
                    
                    // Get day of week for color
                    const dayOfWeek = date.getDay();
                    const dayColors = {
                        0: 'bg-red-500',      // Sunday
                        1: 'bg-yellow-500',   // Monday
                        2: 'bg-pink-500',     // Tuesday
                        3: 'bg-green-500',    // Wednesday
                        4: 'bg-orange-500',   // Thursday
                        5: 'bg-blue-500',     // Friday
                        6: 'bg-purple-500'    // Saturday
                    };
                    const dayColor = dayColors[dayOfWeek] || 'bg-[#A5848F]';
                    
                    const dayNames = ['อาทิตย์', 'จันทร์', 'อังคาร', 'พุธ', 'พฤหัสบดี', 'ศุกร์', 'เสาร์'];
                    const dayName = dayNames[dayOfWeek];
                %>
                <div class="job bg-white shadow-sm hover:shadow-md transition-shadow rounded-lg p-4 border border-gray-200 cursor-pointer group" 
                    onclick="openPopup(<%= index %>)">
                    <div class="space-y-3">
                        <!-- Date and Time -->
                        <div class="flex items-center gap-3">
                            <div class="<%= dayColor %> text-white rounded-lg px-3 py-1.5 text-sm font-medium">
                                <%= dayName %> <%= date.toLocaleDateString('th-TH', { day: '2-digit', month: '2-digit', year: 'numeric' }) %>
                            </div>
                            <span class="text-gray-700 text-base font-medium"><%= hhmm %> น.</span>
                            
                            <!-- Status Badge/Button -->
                            <div class="ml-auto" onclick="event.stopPropagation()">
                                <% if (appointment.status === 'booked') { %>
                                    <form method="POST" action="/employee/update-status/<%= appointment.appointment_id %>" style="display: inline;">
                                        <input type="hidden" name="status" value="in_progress">
                                        <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors">
                                            เริ่มทำงาน
                                        </button>
                                    </form>
                                <% } else if (appointment.status === 'in_progress') { %>
                                    <span class="inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium bg-blue-100 text-blue-800">
                                        🔄 กำลังดำเนินการ
                                    </span>
                                <% } else if (appointment.status === 'completed') { %>
                                    <span class="inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium bg-green-100 text-green-800">
                                        ✓ เสร็จสิ้น
                                    </span>
                                <% } else if (appointment.status === 'canceled') { %>
                                    <span class="inline-flex items-center px-3 py-1.5 rounded-lg text-sm font-medium bg-red-100 text-red-800">
                                        ✗ ยกเลิก
                                    </span>
                                <% } %>
                            </div>
                        </div>
                        <% 
                            // parse extras for this appointment (if present as JSON string)
                            let _extrasObj = null;
                            try {
                                _extrasObj = appointment.extrasObj || (appointment.extras ? JSON.parse(appointment.extras) : null);
                            } catch (e) { _extrasObj = null; }
                            let _extrasTotal = 0;
                            if (_extrasObj && Array.isArray(_extrasObj)) {
                                _extrasObj.forEach(ex => { _extrasTotal += parseFloat(ex.cost) || 0; });
                            }
                        %>

                        <% if (_extrasObj && _extrasObj.length) { %>
                        <div class="mt-3">
                            <label class="block text-sm font-medium text-gray-700 mb-1">บริการเสริม</label>
                            <ul class="list-disc ml-5 text-sm text-gray-700">
                                <% _extrasObj.forEach(function(ex) { %>
                                    <li><%= ex.name %> — <%= ex.cost %> บาท</li>
                                <% }); %>
                            </ul>
                            <div class="text-sm font-semibold text-gray-800 mt-1">ยอดบริการเสริม: <%= _extrasTotal %> บาท</div>
                        </div>
                        <% } %>

                        <!-- Appointment Details -->
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-3">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">รายการ</label>
                                <input type="text" value="<%= appointment.Service.name %>" readonly
                                    class="w-full border border-gray-300 rounded-lg px-3 py-2 text-base bg-gray-50">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">ผู้ใช้บริการ</label>
                                <input type="text" value="<%= appointment.User ? appointment.User.first_name + ' ' + appointment.User.last_name : '-' %>" readonly
                                    class="w-full border border-gray-300 rounded-lg px-3 py-2 text-base bg-gray-50">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">ผลการให้บริการ</label>
                                <input type="text" value="<%= appointment.showstatus %>" readonly
                                    class="w-full border border-gray-300 rounded-lg px-3 py-2 text-base bg-gray-50 <%= appointment.status === 'completed' ? 'text-green-700' : appointment.status === 'canceled' ? 'text-red-700' : 'text-gray-900' %>">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">รายได้</label>
                                <input type="text" value="<%= appointment.employeeIncome > 0 ? appointment.employeeIncome.toLocaleString('th-TH', { minimumFractionDigits: 2 }) + ' บาท' : '-' %>" readonly
                                    class="w-full border border-gray-300 rounded-lg px-3 py-2 text-base bg-gray-50 font-medium text-[#A5848F]">
                            </div>
                        </div>
                    </div>
                </div>
            <% }); %>
            <% } %>
        </div>
    </main>

    <!-- Appointment Detail Popup -->
    <div id="popup" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-lg mx-4 relative">
            <!-- Modal Header -->
            <div class="sticky top-0 bg-white border-b border-gray-200 px-6 py-4 rounded-t-lg flex items-center justify-between">
                <h3 class="text-xl font-semibold text-gray-900">ข้อมูลลูกค้า</h3>
                <button class="text-gray-400 hover:text-gray-600 text-2xl leading-none" onclick="closePopup()">×</button>
            </div>
            
            <!-- Modal Body -->
            <div class="p-6 space-y-4">
                <!-- Customer Info -->
                <div class="bg-gray-50 rounded-lg p-4 space-y-2 text-base" id="customerInfo">
                    <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900">ชื่อ:</span>
                        <span class="text-gray-700" id="customerName">-</span>
                        <span class="font-semibold text-gray-900 ml-4">ชื่อเล่น:</span>
                        <span class="text-gray-700" id="customerNickname">-</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900">เบอร์โทร:</span>
                        <span class="text-gray-700" id="customerPhone">-</span>
                    </div>
                    <div class="flex items-center gap-2">
                        <span class="font-semibold text-gray-900">Email:</span>
                        <span class="text-gray-700" id="customerEmail">-</span>
                    </div>
                </div>

                <!-- Service Details -->
                <div>
                    <h4 class="text-base font-semibold text-gray-900 mb-3">รายละเอียด</h4>
                    <div class="space-y-3">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">รายการ</label>
                            <input type="text" id="serviceName" value="" readonly
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 text-base bg-gray-50">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">ราคาบริการ</label>
                            <div class="flex items-center gap-2">
                                <input type="text" id="servicePrice" value="" readonly
                                    class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 text-base bg-gray-50">
                                <span class="text-gray-700 text-base">บาท</span>
                            </div>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">บริการเสริม</label>
                            <div id="extrasList" class="text-sm text-gray-700">-</div>
                            <div class="text-sm font-medium text-gray-900 mt-1">ยอดบริการเสริม: <span id="extrasTotal">-</span> บาท</div>
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">สถานะการให้บริการ</label>
                            <input type="text" id="serviceStatus" value="" readonly
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 text-base bg-gray-50">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">รายได้ของคุณ</label>
                            <div class="flex items-center gap-2">
                                <input type="text" id="employeeIncome" value="" readonly
                                    class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 text-base bg-gray-50 font-medium text-[#A5848F]">
                                <span class="text-gray-700 text-base">บาท</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Modal Footer -->
            <div class="border-t border-gray-200 px-6 py-4">
                <button onclick="closePopup()" class="w-full bg-gray-100 hover:bg-gray-200 text-gray-700 px-6 py-2.5 rounded-lg font-medium transition-colors text-base">
                    ปิด
                </button>
            </div>
        </div>
    </div>

    <!-- ทำfilter -->
    <script>
        // Appointment data from server
        const appointmentsData = [
            <% appointments.forEach((appointment, index) => { %>
                {
                    customerName: '<%= appointment.User ? (appointment.User.first_name + " " + appointment.User.last_name) : "-" %>',
                    customerNickname: '<%= appointment.User && appointment.User.nickname ? appointment.User.nickname : "-" %>',
                    customerPhone: '<%= appointment.User && appointment.User.phone ? appointment.User.phone : "-" %>',
                    customerEmail: '<%= appointment.User && appointment.User.email ? appointment.User.email : "-" %>',
                    serviceName: '<%= appointment.Service ? appointment.Service.name : "-" %>',
                    servicePrice: '<%= appointment.Service ? parseFloat(appointment.Service.price).toLocaleString("th-TH", { minimumFractionDigits: 2 }) : "0.00" %>',
                    status: '<%= appointment.showstatus %>',
                    employeeIncome: '<%= appointment.employeeIncome > 0 ? appointment.employeeIncome.toLocaleString("th-TH", { minimumFractionDigits: 2 }) : "0.00" %>',
                    <% 
                        // prepare extras text and total for popup
                        let _ed = '';
                        let _et = 0;
                        try {
                            const _eo = appointment.extrasObj || (appointment.extras ? JSON.parse(appointment.extras) : null);
                            if (_eo && Array.isArray(_eo)) {
                                _ed = _eo.map(x => (x.name + ' (' + x.cost + '฿)')).join(', ');
                                _et = _eo.reduce((s, x) => s + (parseFloat(x.cost) || 0), 0);
                            }
                        } catch(e) { _ed = ''; _et = 0; }
                    %>
                    extrasText: '<%= _ed %>',
                    extrasTotal: '<%= _et.toFixed(2) %>'
                }<%= index < appointments.length - 1 ? ',' : '' %>
            <% }); %>
        ];

        function openPopup(index) {
            const appointment = appointmentsData[index];
            
            // Populate customer info
            document.getElementById('customerName').textContent = appointment.customerName;
            document.getElementById('customerNickname').textContent = appointment.customerNickname;
            document.getElementById('customerPhone').textContent = appointment.customerPhone;
            document.getElementById('customerEmail').textContent = appointment.customerEmail;
            
            // Populate service details
            document.getElementById('serviceName').value = appointment.serviceName;
            document.getElementById('servicePrice').value = appointment.servicePrice;
            document.getElementById('serviceStatus').value = appointment.status;
            document.getElementById('employeeIncome').value = appointment.employeeIncome;
            // Populate extras
            const exList = document.getElementById('extrasList');
            const exTotal = document.getElementById('extrasTotal');
            if (exList) exList.textContent = appointment.extrasText && appointment.extrasText.length ? appointment.extrasText : '-';
            if (exTotal) exTotal.textContent = appointment.extrasTotal ? Number(appointment.extrasTotal).toFixed(2) : '0.00';
            
            document.getElementById("popup").classList.remove("hidden");
            document.getElementById("popup").classList.add("flex");
        }
        
        function closePopup() {
            document.getElementById("popup").classList.add("hidden");
            document.getElementById("popup").classList.remove("flex");
        }
        
        function openPopupprofile(){
            document.getElementById("popuppro").classList.remove("hidden");
            document.getElementById("popuppro").classList.add("flex");
        }
        
        function closePopupprofile() {
            document.getElementById("popuppro").classList.add("hidden");
        }
        
        function previewImage(event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    const preview = document.getElementById('preview');
                    preview.src = e.target.result;
                    preview.classList.remove('hidden');
                }
                reader.readAsDataURL(file);
            }
        }

    </script>

    <%- include('emp_profile_popup') %>

</body>
</html>